[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "credibilis_credit"
version = "0.1.0"
description = "Moteur Métier Crédit CREDIBILIS — Grille d'évaluation et règles d'octroi UEMOA"
authors = [{ name = "SahelTech" }]
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
