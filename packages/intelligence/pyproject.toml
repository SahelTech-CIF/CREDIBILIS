[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "credibilis_intelligence"
version = "0.1.0"
description = "Couche d'Abstraction Intelligence Artificielle & Fournisseurs LLM CREDIBILIS"
authors = [{ name = "SahelTech" }]
dependencies = [
    "pydantic>=2.0.0"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
